@model ZlatnaRekolta.Data.Order

@{
    ViewData["Title"] = "Изтриване";
}

<h1 class="text-danger">Изтриване на артикул от кошницата</h1>

<h4>Сигурни ли сте, че искате да изтриете следния артикул?</h4>
<hr />
<div class="card shadow p-4 bg-light">
    <dl class="row">
        <dt class="col-sm-4">Продукт:</dt>
        <dd class="col-sm-8">@Model.Products?.Name</dd>

        <dt class="col-sm-4">Количество:</dt>
        <dd class="col-sm-8">@Model.Quantity @Model.Products.UnitOfMe</dd>

        <dt class="col-sm-4">Описание:</dt>
        <dd class="col-sm-8">@Model.Description</dd>

        <dt class="col-sm-4">Цена:</dt>
        <dd class="col-sm-8">@Math.Round(Model.Products.Price * Model.Quantity, 2) лв.</dd>
    </dl>
</div>
<br />
<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">
        🗑 Потвърди изтриването
    </button>
    <a asp-action="Index" class="btn btn-dark ms-2">
        Назад към списъка
    </a>
</form>